# Deploy su dev.francescomelani.com (Hostinger) via FTP
# Hostinger blocca SSH/SFTP dai server GitHub → usiamo FTP che funziona da ovunque.
# Si attiva su push sul branch main, oppure manualmente.

name: Deploy to dev.francescomelani.com

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to Hostinger (FTP)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.HOSTINGER_FTP_SERVER }}
          username: ${{ secrets.HOSTINGER_FTP_USER }}
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          server-dir: ${{ format('{0}/', secrets.HOSTINGER_FTP_REMOTE_DIR) }}
          local-dir: ./
          exclude: |
            .git/
            .github/
            node_modules/
            .env
            .env.*
            *.log
            .gitignore
          # Rimuove dal server file non presenti nel repo (template Hostinger, vecchia cartella deploy/)
          dangerous-clean-slate: true
